
fn main(canary: i64) i64 = {
    var output: List_u8 = list();
    // TODO: it goes away if you dont have a binding
    let _ = @fmt(output&, "Hello %! %", "World", "abc");
    var words = output&.items();
    print_int(words&.len[]);
    puts(words);

    canary
}