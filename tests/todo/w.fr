// same as tests/todo/b.ssa
// https://lists.sr.ht/~mpu/qbe/%3C3BM57P0KYZ41N.33ET0S1FSPNYI@mforney.org%3E
//fn uninit_stack_slot() #test = {
main :: fn() void = {
    f :: fn() i64 = {
        c :: @static(i64) 0;
        if(c[] != 0, => return(1));
        c[] = 1;
        0
    };
    x_is_zero :: fn() void #noinline = ();
    x := @uninitialized Ty(i64, i64);
    loop {
        if f() != 0 {
            if x._0 == 0 {
                x_is_zero();
            };
            return();
        } else {
            x._0 = 1;
        };
    };
}
