
main :: fn() void = {
    // TODO: type error is on calling f instead of assigning y2=a
    a: f64 = 1.0;
    (x1: f32, x2: f32, y1: f32, y2: f32) := (0, 1, 0, a);
    f :: fn(a: f32) void = ();
    f(y2);
}