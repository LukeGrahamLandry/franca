
main :: fn() void = {
    Pin :: Ty(i16, i16);
    // TODO: why the fuck does this one need an extra cast :compiler
    h_line :: @const_slice(@as(Pin) (-1, 0), (1, 0));
    // works if you do this:
    //h_line :: @const_slice(@as(Pin) (-1, 0), (@as(i16) 1, 0));
    h_line;
}
