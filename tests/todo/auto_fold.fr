// similar to todo/fold_string_escapes.fr?
main :: fn() void = {
    T :: @struct(a: i64);
    a := zeroed(Array(i64, 5));
    // Compile Error: attempted to call ComptimeOnly function [operator_star_prefix F696 at compiler:36:28] at runtime
    // works if you add an @run
    a := bit_cast_unchecked(@type a&, /*@run*/ *Array(T, 5), a&);
    @assert_eq(a[0].a, 0);
}
