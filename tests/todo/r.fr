
// This typechecks but emits as a call with 2 i64 args 
// but the func expects 1 Ty(i64,i64) parc 
// (and fails a debug check when inlining)
// compile_for_arg needs to check if unary instead of checking if arg is tuple
main :: fn() void = {
    who_is :: fn(value: Ty(i64, i64)) i64 = value._0 + value._1;
    assert_eq(3, who_is(@as(Ty(i64, i64)) (1, 2)));
}
