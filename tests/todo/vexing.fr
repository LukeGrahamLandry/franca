// Compile Error: Type Error: tuple arity mismatch. expected 10 but found 3 (spread=false)
main :: fn() void = {
    // this works:
    a: Array(i64, 10) = (0, 1, ..(-1));
    // this doesn't: 
    a: Array(i64, 10) = (0, 1, ..-1);
    @assert_eq(a&[2], -1);
}
