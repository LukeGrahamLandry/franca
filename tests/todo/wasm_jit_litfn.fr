// franca examples/web/build.fr && franca examples/import_wasm/run.fr target/web/target/demo.wasm -- -lang franca -file tests/todo/wasm_jit_litfn.fr
// >>> panic! wasm guest called a null function pointer
// lit_fn not getting compiled in time?
// - explicit @run on the block makes it work and so does wrapping the block in a function.
// - works with `S :: u8;` but not `S :: u32;` or `S :: new struct;`
// - works if a's first argument is FuncId instead of fnptr

main :: fn() void = {
    S :: @struct(_: u8);
    a :: fn(_: @FnPtr() void, __: []S) void = ();
    b :: fn() void = ();
    c :: a(b, /* @run */ {  
        o: []S = empty();
        o
    });
    c;
}
