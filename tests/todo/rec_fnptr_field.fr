main :: fn() void = {
    S :: @rec @struct {
        a: C;
    };
    C :: @FnPtr(s: *S) void;
    
    // Compile Error: Poison expression InProgressMacro. 
    C;
    // it works if you evaluate S first or if you put @rec on C. 
}

