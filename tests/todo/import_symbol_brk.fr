
// run with FRANCA_NO_CACHE=1 
// `trace trap: compiler inserted break instruction`
// TODO: this was when FRANCA_NO_CACHE meant ExecStyle.JIT, 
//       which is no longer true so it works now and it also works at comptime. 
//       maybe just to be safe try JitOnly + got_indirection_instead_of_patches=false, 
//       before promoting it to a test. 
main :: fn() void = {
    println(!(@import_symbol "exit").is_null());
}
