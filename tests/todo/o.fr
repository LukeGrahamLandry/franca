main :: fn() void = {
    E :: @tagged(A: void);
    Foo :: @struct {
        bar :: 123;
    };
    f :: fn(a: u32, foo: E) Ty(u32, u32) = {
        @match(foo) {
            fn A() => {
                r := 999;
                yield := Foo.bar;
                // This shouldn't compile. 
                r
            }
        }
    };
    a, b := f(1, .A);
    @println("% %", a, b);
}
