 main :: fn() void = {
    FOO :: @struct {
        important_value :: 123;
    };
 
    sss := 1;
    if true {
        #use(FOO);
        a := sss + important_value;
    };
    
    // TODO: if this isn't in a new scope :compilerbug
    //       Unknown type for runtime var sss%36101%54103
    //       its something like use_in_nested_capturing_call, only happens with the #use above
    //{
        sss := 0;
    //};
 }
 
 