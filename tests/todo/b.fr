
fn main() = {
    // It's fine for this to not work but rn it's for the wrong reason. 
    MTLViewport :: @struct(originX: f64);
    x: i32 = 0;
    vp: MTLViewport = (
        originX = float intcast x,
    );
}