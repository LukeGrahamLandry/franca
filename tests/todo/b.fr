// Compile Error: You can't have a void pointer 324 as a constant. The compiler can't tell how many bytes to put in the final executable.
main :: fn() void = {
    // this becomes `:: @slice(ast_alloc())` instead of `::@slice(@as(void) ()) (ast_alloc())`
    a := @const_slice();
    ::debug_log_type(@type a);
}
