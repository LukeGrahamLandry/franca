main :: fn() void = {
    s := zeroed LinuxLibc.DirEnt;
    foo :: fn(it: *LinuxLibc.DirEnt, name: []u8) void #noinline = {
        // panic! getalias bad ref RNull in $foo__3552
        it.d_type = todo();
        it.d_name&.items().slice(0, name.len).copy_from(name);
        it.d_name&[name.len] = 0;
    }
    foo(s&, empty());
}
