
// TODO: make sure backtrace goes to the right line (rn it works if not @run). 
//       @run doesn't even show location :(
main :: fn() = @run {
    //Crash :: import("@/lib/crash_report.fr");
    //Crash'hook_backtrace();
    @assert(false);
};