
main :: fn() void = {
    A :: @tagged(a: i64, b: Str);
    B :: @struct(c: A #use);
    s: B = (c = (a = 5));
    
    //Compile Error: Place expression of type *@struct(c: @tagged(a: i64,b: Str,),) expected pointer dereference.
    @if_let(s&) fn b() => unreachable();
    
    // this works tho
    //s := s&;
    //@if_let(s) fn b() => unreachable();
}
