// i've been ignoring this for so long but it's so embarasing i should really fix it
main :: fn() void = {
    a := true;
    // Compile Error: overload not found in 'if'; wanted: (bool, ) -> ???,
    if (a && a) || a {| };
    if (a || a) && a {| };

    // this works tho    
    if a || (a && a) {| };
}
