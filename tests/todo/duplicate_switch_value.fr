// Expr.Switch (and thus @match) does it right but @switch compiles to == chain so it works on non-ints
main :: fn() void = {
    a := 0;
    @switch(a) {
        @case(0) => println("A");
        @case(0/* oops */) => println("B");
        @default => println("C");
    }
}
