
main :: fn() = {
    void.loop() { $break | 
        //    break :: fn(result: T) Never => return(result);
        //Compile Error: Unknown type for runtime var result%1483%3231
        // arg_types/tuple_types [] vs [void]. fn(); vs fn(t: void);
        //break();
        
        //        break(@as(void) ());
        //Compile Error: TODO: resolve_in_overload_set as 0
        // hard to decide what to do here. void macro target means no second argument. 
        //break(@as(void) ());
        
        //    break :: fn(result: T) Never => return(result);
        //Type Error: expected Never but found void
        v := ();
        @switch(0) {
            @case(1) => ();
            @default => break(v);
        };
        
        // void.const_eval(@{}) should probably be #fold
    };
}
