// TODO: have a less dumb register allocator. 
//       this needs to not use an O.swap (3 instruction on arm)
//       (this is a code quality problem, not a compiler bug)

main :: fn() void = {
    test :: fn(x: i64, y: i64, z: i64, w: i64) i64 #noinline #log_ir("RD") = {
        //x+y+z+w  // one swap
        x+z+y+w  // two swaps
    };
    test(1,1,1,1);
}
