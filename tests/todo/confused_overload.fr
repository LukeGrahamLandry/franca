
main :: fn() void = {
    start := @my_list(1) 2;
    
    fn my_list(); 
    
    // Compile Error: Poison expression Argument. 
    fn my_list($T: Type, a: i64) Foo(T) #generic = 
        ();
    
    Foo :: fn(T: Type) Type = void;
    
    fn my_list(a: FatExpr, b: FatExpr) FatExpr #macro = 
        @literal 0;
}
