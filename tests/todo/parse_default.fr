// Parse Error: non-update-assingment operator after expression statement. this should be unreachable?
main :: fn() void = {
    name := "a";
    allow_null := true;
    @switch(name) {
        @default => if !allow_null {
            println("Hello");
        };
    };
}
