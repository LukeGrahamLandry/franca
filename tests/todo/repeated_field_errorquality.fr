
// correctly errors but the message is misleading 
// "Compile Error: ICE: struct field count mismatch but we don't know which is missing"
// should just say that you repeated a field
main :: fn() void = {
    S :: @struct(a: i64);
    s: S = (a = 1, a = 2);
}