// debug assert. panic! wasm signature mismatch $a__2450
// (when aot with default_driver, not jit by demo.wasm)
main :: fn() void = {
    a :: fn() i64 = {
        println("hello");
        Syscall'exit(0);
        123
    };
    b := a();
};
