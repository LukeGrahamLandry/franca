
main :: fn() void = {
    out, err := ("\n", "");
    // Compile Error: attempted to call ComptimeOnly function [str F3796 at compiler:51:11] at runtime
    @assert(out == "\n" && err.len == 0);
  
    // - it works if you flip the order:
    //   @assert(err.len == 0 && out == "\n");
    // - it works if the string doesn't need __string_escapes. 
    // - it works if it's not in a macro: 
    //   x := (out == "\n" && err.len == 0);
}
