
main :: fn() void = {
    S :: @struct(a := 123);
    
    // since this gets the default value...
    s: S = ();
    assert_eq(s.a, 123);
    
    // it looks like this would be the same, 
    ss := @static(S) ();
    assert_eq(ss.a, 0);  
    
    // but that's treated the same as @static(S); which gets a zeroed initial value. 
    // probably i just shouldn't allow bare @static, 
    // same way i force variables to have an initializer expression. 
    // have it just be one expression and if it needs type hint you @as (like @ref). 
}
