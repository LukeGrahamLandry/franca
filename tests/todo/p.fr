
// This is not a compiler bug but it is quite a footgun. 
// not sure if there's anything sane i can do to improve this. 
// maybe you shouldn't be able to print void. 
main :: fn() void = {
    a := {
        1;
    };
    // a is void not int
    @println("%", a);
}

