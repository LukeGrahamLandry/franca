data $empty = { b 0 }

export function w $main() {
@start
    %f =l sub $strlen, 1
    %a =l sub $empty, 1
    %r =l call $inc_call(l %f, l $empty)
    ret %r
}

function l $inc_call(l %f, l %a) {
@start
    %g =l add %f, 1
    %b =l add %a, 1
    %r =l call %g(l %b)
    ret %r
}
