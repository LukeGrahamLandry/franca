

#test
fn load_store_u8() = {
    a := 257;
    ptr: *u8 = a&; 
    b := ptr[];
    assert_eq(@as(i64) b, 1);
    assert_eq(a, 257);
    ptr[] = @as(u8) 2;
    assert_eq(a, 258);
    
    assert_eq(u8.size_of(), 1);
}
