@import(interp, system);

const CmdResult: Type;
fn system(cmd: Str) CmdResult;

const putchar;

fn main(_canary: i64) i64 = {
    let dir = system("ls");
    puts(dir.stdout[]);
    puts("via c call: ");
    putchar(65)!c_call;
    putchar(66)!c_call;
    putchar(67)!c_call;
    putchar(10)!c_call;
    
    _canary
}
