// this one's not a crippling bug but its pretty cringe that i can't do it. 

#test(aarch64)
fn main(canary: i64) i64 = {
    fn add_slow(a: i64, b: i64) i64 = 
        if(b.eq(0), fn = a, fn = add_slow(a.add(1), b.sub(1)));
    
    N :: 9999999999;
    assert_eq(N, add_slow(0, N));
    
    canary
}
