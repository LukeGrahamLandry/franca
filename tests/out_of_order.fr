#test(aarch64)
fn main(canary: i64) i64 = {
    assert_eq(two(), 5);
    
    fn two() i64 = one();
    fn one() i64 = FIVE;
    const FIVE = FOUR.add(1);
    const FOUR = THREE.add(1);
    
    canary
}

const THREE = three;
const three = 3;
