#include_std("lib/core.fr"); 
#include_std("compiler/lib.fr"); 

//! Entry point that uses new self hosted sema instead of the original rust one. 
//! For now, keep it easy to switch between them by just poking different implementations into the vtable. 
//! TODO: When the new version actually works, go through and see what indirection can be removed. 
fn main() void = {
    vtable := new_sema_init_vtable();
    self_hosted_main(vtable, true);
}
