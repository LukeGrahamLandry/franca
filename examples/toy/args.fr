fn main(argc: i64, argv: *CStr) = {
    //println("Hello World");
    
    //out: List(u8) = list(256, page_allocator);
    //println("AAA");
    //path := "/proc/self/cmdline".sym().c_str();
    //flag := (@as(i64) OpenFlag.Read);
    //f := open(path, flag).unwrap();
    //read_len := read(f, out.maybe_uninit.ptr, out.maybe_uninit.len).unwrap();
    //out.len += read_len;
    //close(f); // TODO: defer
    //println(out.items());

    args: []CStr = (ptr = argv, len = argc);
    for args { a | 
        @println("%", a);
    };
    
    exit(0);   
}
