// expected output; arm/amd: true, riscv: false, wasm: runtime error. 
main :: fn() void = {
    x: i32 = -1;
    x: i64 = x.zext();
    g := bit_cast_unchecked(@FnPtr(i32) void, @FnPtr(i64) void, f);
    g(x);
    f :: fn(x: i32) void = println(x == -1);
}
