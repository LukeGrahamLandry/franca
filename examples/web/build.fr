driver :: fn(vtable: *ImportVTable) void = {
    franca := get_executable_path(temp());
    run_cmd_blocking(franca, @slice("examples/default_driver.fr", "build", "examples/kaleidoscope.fr", "-o", "examples/web/a.wasm", "-arch", "wasm32"));
}

#use("@/lib/sys/process.fr");
#use("@/lib/sys/subprocess.fr");
