
// stress test for getting preempted mid-syscall
main :: fn() void = {
    size := page_size();
    range(0, 1000000) { _ |
        buf := page_allocator.alloc_uninit(u8, size);
        page_allocator.dealloc(u8, buf);
    };
    print("ok\n");
}
