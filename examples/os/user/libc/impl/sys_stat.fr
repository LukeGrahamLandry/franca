
open :: fn(path: CStr, flags: i32, va: CVariadic) i32 = {
    flags := flags.intcast();
    r := Posix'openat(Posix'AT'FDCWD(), path, flags, 0);
    @if(r&.is_err()) return(-r.Err._.abs().intcast());
    r.Ok.fd
}

#use("@/lib/variadic.fr");
