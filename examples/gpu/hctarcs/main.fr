
main :: fn() void = @run {
    p := import("@/examples/gpu/hctarcs/tests/mandelbrot.fr")'build();
    out := u8.list(temp());
    p.build(out&);
    write :: import("@/lib/sys/fs.fr").write_entire_file_or_crash;
    write("target/a.json", out.items());
}
