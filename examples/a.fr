#use("@/lib/sys/jump.fr")
main :: fn() #log_ir("R") = {
    buf := JumpBuf.zeroed();
    @match_try(buf&, {
        println("A");
        throw(buf&);
    }, {
        println("B");
    });
    println("C");
}
