export
function w $add(w %a, w %b) {
@start
    %c =w add %a, %b
    ret %c
}

data $hello_world = { b "Hello World!" }

export
function $main() {
@start
    call $write$host(w $hello_world, w 12)
    ret
}
