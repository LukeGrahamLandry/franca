name: tests
on: [push]

jobs:
  test:
    name: linux --cranelift
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo run --no-default-features --features cranelift -- --cranelift
