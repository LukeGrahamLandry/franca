//! TODO: I want fn hash to just be an overload set so you can provide your own way of walking your fields. 
//!       but I can't have arg generics in an overload set yet :(

auto_hash :: fn(const H: Type, const T: Type, hasher: *H, self: *T) Unit = {
    byte = ptr_cast_unchecked(From = i64, To = u8, ptr = self);
    hasher.write((ptr = byte, len = i64.size_of()));
}


