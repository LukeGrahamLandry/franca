# arm64 hfa bug in qbe

type :hfa4 = { d,d,d,d }

export
function $f(:hfa4 %h, l %null) {
@start
        call $phfa4(:hfa4 %h)
        ret
}

# >>> driver
# #include <stdio.h>
# typedef struct { double f1, f2, f3, f4; } hfa4;
# void f(hfa4, long);
# void phfa4(hfa4 h) { printf("{ %g, %g, %g, %g }\n", h.f1, h.f2, h.f3, h.f4); }
# int main() {
#       hfa4 h={1,2,3,4};
#       f(h, 0);
# }
# <<<

# >>> output
# { 1, 2, 3, 4 }
# <<<
